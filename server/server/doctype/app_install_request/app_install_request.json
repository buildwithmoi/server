{
 "actions": [],
 "autoname": "format:AIR-{#####}",
 "creation": "2026-08-24 00:00:00",
 "doctype": "DocType",
 "engine": "InnoDB",
 "field_order": [
  "operation",
  "bench",
  "column_break_head",
  "app_name",
  "branch",
  "source_section",
  "source_type",
  "github_profile",
  "repo",
  "column_break_source",
  "git_url",
  "resolved_git_url",
  "bench_command",
  "command_params",
  "ssl_section",
  "ssl_mode",
  "ssl_domain",
  "ssl_dry_run",
  "restore_section",
  "restore_source",
  "restore_backup_key",
  "restore_database_file",
  "restore_public_file",
  "restore_private_file",
  "restore_backup_first",
  "restore_public_files",
  "restore_private_files",
  "restore_db_username",
  "restore_db_password",
  "restore_encryption_key",
  "options_section",
  "skip_assets",
  "overwrite_existing",
  "allow_merge",
  "column_break_options",
  "install_on_site",
  "force_install",
  "console_section",
  "console_command",
  "provision_section",
  "provision_frappe_version",
  "provision_bench_name",
  "provision_site_name",
  "provision_apps",
  "provision_port_index",
  "provision_skip_assets",
  "provision_domain",
  "provision_domain_provider",
  "provision_db_password",
  "provision_admin_password",
  "status_section",
  "status",
  "exit_code",
  "started_at",
  "column_break_status",
  "finished_at",
  "duration",
  "job_id",
  "log_section",
  "steps",
  "error_summary",
  "command",
  "output"
 ],
 "fields": [
  {
   "default": "Clone",
   "description": "Clone runs `bench get-app` to bring a repository into the bench for the first time. Pull runs `git pull` inside an app already there. Command runs one of the catalogued bench commands. SSL issues or renews a Let's Encrypt certificate. They are different enough that the choice is always explicit rather than inferred from which fields happen to be filled in. Console runs an arbitrary command in the bench directory \u2014 the one operation that is not assembled from a fixed argv, which is why every use of it is recorded as a Security Event.",
   "fieldname": "operation",
   "fieldtype": "Select",
   "in_list_view": 1,
   "in_standard_filter": 1,
   "label": "Operation",
   "options": "Clone\nPull\nCommand\nSSL\nRestore\nConsole\nProvision",
   "reqd": 1
  },
  {
   "fieldname": "bench",
   "fieldtype": "Link",
   "in_list_view": 1,
   "in_standard_filter": 1,
   "label": "Bench",
   "options": "Server Bench",
   "reqd": 0,
   "description": "Empty only while a Provision request is building the bench it will point at \u2014 every other operation acts on a bench that already exists."
  },
  {
   "fieldname": "column_break_head",
   "fieldtype": "Column Break"
  },
  {
   "description": "For a Pull this is the app directory inside the bench. For a Clone it is derived from the repository name and passed to bench explicitly, so bench never has to guess it from a URL carrying an SSH host alias.",
   "fieldname": "app_name",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "App"
  },
  {
   "description": "Leave blank to use the repository's default branch for a Clone, or the branch the app is already on for a Pull.",
   "fieldname": "branch",
   "fieldtype": "Data",
   "label": "Branch"
  },
  {
   "depends_on": "eval:doc.operation==\"Clone\"",
   "fieldname": "source_section",
   "fieldtype": "Section Break",
   "label": "Source"
  },
  {
   "default": "GitHub Profile",
   "depends_on": "eval:doc.operation==\"Clone\"",
   "description": "A profile supplies the account, the SSH host alias and the token used to list repositories. Use Git URL for a remote that is not on one of your profiles.",
   "fieldname": "source_type",
   "fieldtype": "Select",
   "label": "Source Type",
   "options": "GitHub Profile\nGit URL"
  },
  {
   "depends_on": "eval:doc.operation==\"Clone\" && doc.source_type==\"GitHub Profile\"",
   "fieldname": "github_profile",
   "fieldtype": "Link",
   "label": "GitHub Profile",
   "options": "GitHub Profile"
  },
  {
   "depends_on": "eval:doc.operation==\"Clone\" && doc.source_type==\"GitHub Profile\"",
   "description": "Just the repository name, e.g. gh_erp \u2014 not the full URL.",
   "fieldname": "repo",
   "fieldtype": "Data",
   "label": "Repository"
  },
  {
   "fieldname": "column_break_source",
   "fieldtype": "Column Break"
  },
  {
   "depends_on": "eval:doc.operation==\"Clone\" && doc.source_type==\"Git URL\"",
   "description": "A full git remote, e.g. git@github.com:org/repo.git",
   "fieldname": "git_url",
   "fieldtype": "Data",
   "label": "Git URL"
  },
  {
   "depends_on": "eval:doc.operation==\"Clone\"",
   "description": "The exact remote that will be cloned, after any host alias is applied.",
   "fieldname": "resolved_git_url",
   "fieldtype": "Data",
   "label": "Resolved URL",
   "read_only": 1
  },
  {
   "fieldname": "options_section",
   "fieldtype": "Section Break",
   "label": "Options"
  },
  {
   "default": "1",
   "depends_on": "eval:doc.operation==\"Clone\"",
   "description": "Leave this on. Building assets is by far the largest memory consumer in a bench operation and the step most likely to take a small server down. Run `bench build --app <name>` by hand when the box is idle.",
   "fieldname": "skip_assets",
   "fieldtype": "Check",
   "label": "Skip Asset Build"
  },
  {
   "depends_on": "eval:doc.operation==\"Clone\"",
   "description": "If the app directory already exists, replace it. bench ARCHIVES the old copy to archived/apps/<name>-<date> rather than deleting it, so nothing is lost.",
   "fieldname": "overwrite_existing",
   "fieldtype": "Check",
   "label": "Overwrite If Present"
  },
  {
   "depends_on": "eval:doc.operation==\"Pull\"",
   "description": "By default a Pull is --ff-only, so it refuses rather than inventing a merge commit when the branch has diverged. Tick this only if you know the divergence is expected and a merge is what you want.",
   "fieldname": "allow_merge",
   "fieldtype": "Check",
   "label": "Allow Merge Commit"
  },
  {
   "fieldname": "column_break_options",
   "fieldtype": "Column Break"
  },
  {
   "depends_on": "eval:[\"Clone\",\"Command\",\"SSL\",\"Restore\"].includes(doc.operation)",
   "description": "For a Clone, optionally run `bench --site <site> install-app` afterwards. For a site-scoped Command, the site it runs against. Leave blank on a Clone to only place the code on disk.",
   "fieldname": "install_on_site",
   "fieldtype": "Data",
   "label": "Site"
  },
  {
   "depends_on": "eval:doc.operation==\"Clone\" && doc.install_on_site",
   "description": "Passes --force to install-app. Only needed when reinstalling an app the site already has.",
   "fieldname": "force_install",
   "fieldtype": "Check",
   "label": "Force Install"
  },
  {
   "fieldname": "status_section",
   "fieldtype": "Section Break",
   "label": "Result"
  },
  {
   "default": "Draft",
   "description": "\u201cCompleted With Warnings\u201d means the work was done but the command still exited non-zero. The case this exists for: bench finishes a get-app by calling `sudo supervisorctl status`, which fails on a host with no passwordless sudo \u2014 the app is cloned and installed, and only the cosmetic restart failed. Reporting that as Failed invites a re-run, and a re-run needs --overwrite, which archives the app that was already fine.",
   "fieldname": "status",
   "fieldtype": "Select",
   "in_list_view": 1,
   "in_standard_filter": 1,
   "label": "Status",
   "options": "Draft\nQueued\nRunning\nSuccess\nCompleted With Warnings\nFailed\nCancelled",
   "read_only": 1
  },
  {
   "description": "The authoritative success signal \u2014 0 succeeded, above 0 the command failed, -1 means it never ran because a pre-flight refused it. stdout and stderr are interleaved in the log for readability, so the log text can never be used to decide whether it worked.",
   "fieldname": "exit_code",
   "fieldtype": "Int",
   "label": "Exit Code",
   "read_only": 1
  },
  {
   "fieldname": "started_at",
   "fieldtype": "Datetime",
   "label": "Started",
   "read_only": 1
  },
  {
   "fieldname": "column_break_status",
   "fieldtype": "Column Break"
  },
  {
   "fieldname": "finished_at",
   "fieldtype": "Datetime",
   "label": "Finished",
   "read_only": 1
  },
  {
   "fieldname": "duration",
   "fieldtype": "Duration",
   "label": "Duration",
   "read_only": 1
  },
  {
   "fieldname": "job_id",
   "fieldtype": "Data",
   "label": "Job ID",
   "read_only": 1
  },
  {
   "fieldname": "log_section",
   "fieldtype": "Section Break",
   "label": "Log"
  },
  {
   "fieldname": "error_summary",
   "fieldtype": "Small Text",
   "label": "Error Summary",
   "read_only": 1
  },
  {
   "description": "The exact argv that was executed, so it can be reproduced by hand in a terminal.",
   "fieldname": "command",
   "fieldtype": "Small Text",
   "label": "Command",
   "read_only": 1
  },
  {
   "fieldname": "output",
   "fieldtype": "Code",
   "label": "Output",
   "options": "Text",
   "read_only": 1
  },
  {
   "depends_on": "eval:doc.operation==\"Command\"",
   "description": "Identifier from the catalogue in server/bench/commands.py. Commands are never assembled from free text \u2014 the argv comes from a fixed entry plus parameters matched against a pattern.",
   "fieldname": "bench_command",
   "fieldtype": "Data",
   "label": "Command",
   "read_only": 1
  },
  {
   "depends_on": "eval:doc.operation==\"Command\"",
   "description": "JSON of the values supplied for this command's parameters, each validated against the pattern its catalogue entry declares.",
   "fieldname": "command_params",
   "fieldtype": "Small Text",
   "label": "Command Parameters",
   "read_only": 1
  },
  {
   "depends_on": "eval:doc.operation==\"SSL\"",
   "fieldname": "ssl_section",
   "fieldtype": "Section Break",
   "label": "SSL"
  },
  {
   "depends_on": "eval:doc.operation==\"SSL\"",
   "description": "Issue Or Reinstall runs `bench setup lets-encrypt <site> -n`, which stops nginx, asks Let's Encrypt for a certificate and writes it into the site config. Renew drives certbot directly rather than `bench renew-lets-encrypt`, because that command asks for confirmation and cannot be answered by a background job.",
   "fieldname": "ssl_mode",
   "fieldtype": "Select",
   "in_list_view": 0,
   "label": "SSL Operation",
   "options": "Issue Or Reinstall\nRenew"
  },
  {
   "depends_on": "eval:doc.operation==\"SSL\" && doc.ssl_mode==\"Issue Or Reinstall\"",
   "description": "Leave empty to certify the site's own domain. Set it to certify one of the extra domains already configured for the site \u2014 it must already be listed there, and it must resolve to this server before Let's Encrypt will issue anything.",
   "fieldname": "ssl_domain",
   "fieldtype": "Data",
   "label": "Custom Domain"
  },
  {
   "default": "0",
   "depends_on": "eval:doc.operation==\"SSL\" && doc.ssl_mode==\"Renew\"",
   "description": "Rehearse the renewal against Let's Encrypt's staging server. Nothing is installed. Worth doing first: Let's Encrypt rate-limits failures per IP and the block outlasts the mistake, so a rehearsal is cheaper than being locked out.",
   "fieldname": "ssl_dry_run",
   "fieldtype": "Check",
   "label": "Dry Run"
  },
  {
   "depends_on": "eval:doc.operation==\"Restore\"",
   "fieldname": "restore_section",
   "fieldtype": "Section Break",
   "label": "Restore"
  },
  {
   "depends_on": "eval:doc.operation==\"Restore\" && doc.restore_source!=\"Chosen Files\"",
   "description": "Identifies one backup set \u2014 the timestamp and site name frappe shares across the database dump, the files tars and the site_config snapshot. The individual paths are resolved from disk when the job runs, so a set that has been rotated away is caught before anything is dropped rather than after.",
   "fieldname": "restore_backup_key",
   "fieldtype": "Data",
   "label": "Backup Set"
  },
  {
   "default": "1",
   "depends_on": "eval:doc.operation==\"Restore\"",
   "description": "Run `bench backup` immediately before restoring. bench restore drops the database and there is no undo, and the moment you want a backup is the moment it is already gone. Leave this on unless you have just taken one yourself.",
   "fieldname": "restore_backup_first",
   "fieldtype": "Check",
   "label": "Back Up First"
  },
  {
   "default": "0",
   "depends_on": "eval:doc.operation==\"Restore\"",
   "description": "Restore the public files tar from the same backup set, if it has one.",
   "fieldname": "restore_public_files",
   "fieldtype": "Check",
   "label": "Restore Public Files"
  },
  {
   "default": "0",
   "depends_on": "eval:doc.operation==\"Restore\"",
   "description": "Restore the private files tar from the same backup set, if it has one.",
   "fieldname": "restore_private_files",
   "fieldtype": "Check",
   "label": "Restore Private Files"
  },
  {
   "depends_on": "eval:doc.operation==\"Restore\"",
   "description": "Database super user. Left empty this is \"root\", which is almost always right.",
   "fieldname": "restore_db_username",
   "fieldtype": "Data",
   "label": "Database Root Username"
  },
  {
   "depends_on": "eval:doc.operation==\"Restore\"",
   "description": "Required. bench restore needs the database super user to drop and recreate the site database, and without it frappe asks on a terminal a background job does not have. Stored encrypted, cleared once the job finishes, and redacted out of the command that is logged.",
   "fieldname": "restore_db_password",
   "fieldtype": "Password",
   "label": "Database Root Password"
  },
  {
   "depends_on": "eval:doc.operation==\"Restore\"",
   "description": "Only for an encrypted backup. It is the `encryption_key` from the site config of the server the backup was taken on \u2014 an encrypted dump cannot be restored without it.",
   "fieldname": "restore_encryption_key",
   "fieldtype": "Password",
   "label": "Backup Encryption Key"
  },
  {
   "default": "Backup Set",
   "depends_on": "eval:doc.operation==\"Restore\"",
   "description": "Backup Set restores one backup frappe wrote, with its files already matched to its database dump. Chosen Files restores three paths picked by hand \u2014 for a backup copied in from another server, or when the database and the files come from different backups.",
   "fieldname": "restore_source",
   "fieldtype": "Select",
   "label": "Restore From",
   "options": "Backup Set\nChosen Files"
  },
  {
   "depends_on": "eval:doc.operation==\"Restore\" && doc.restore_source==\"Chosen Files\"",
   "description": "Absolute path to the database dump. Must be inside the bench directory \u2014 restoring from anywhere else on the server is refused, because this path arrives from a browser. Stored as Small Text rather than Data: a real backup path on a bench with a long site name exceeds the 140 characters a Data column holds, and the insert failed before the job could even be queued.",
   "fieldname": "restore_database_file",
   "fieldtype": "Small Text",
   "label": "Database File"
  },
  {
   "depends_on": "eval:doc.operation==\"Restore\" && doc.restore_source==\"Chosen Files\"",
   "description": "Optional. Absolute path to the public files tar, inside the bench directory. Stored as Small Text rather than Data: a real backup path on a bench with a long site name exceeds the 140 characters a Data column holds, and the insert failed before the job could even be queued.",
   "fieldname": "restore_public_file",
   "fieldtype": "Small Text",
   "label": "Public Files Tar"
  },
  {
   "depends_on": "eval:doc.operation==\"Restore\" && doc.restore_source==\"Chosen Files\"",
   "description": "Optional. Absolute path to the private files tar, inside the bench directory. Stored as Small Text rather than Data: a real backup path on a bench with a long site name exceeds the 140 characters a Data column holds, and the insert failed before the job could even be queued.",
   "fieldname": "restore_private_file",
   "fieldtype": "Small Text",
   "label": "Private Files Tar"
  },
  {
   "description": "The operation broken into named steps, as JSON. Written by the worker and read by the interface, which folds each step's output underneath it. The complete log is still in Output \u2014 this is the structure over it, so a failure can say which part failed rather than only that something did.",
   "fieldname": "steps",
   "fieldtype": "Code",
   "label": "Steps",
   "options": "JSON",
   "read_only": 1
  },
  {
   "fieldname": "console_section",
   "fieldtype": "Section Break",
   "label": "Console",
   "depends_on": "eval:doc.operation==\"Console\""
  },
  {
   "fieldname": "console_command",
   "fieldtype": "Small Text",
   "label": "Command",
   "depends_on": "eval:doc.operation==\"Console\"",
   "description": "Run verbatim by `bash -lc` in the bench directory. stdin is closed, so anything interactive (vim, top, mariadb, bench console) fails immediately rather than hanging. Small Text rather than Data because a real one-liner with a pipeline exceeds Data's 140 characters.",
   "read_only": 1
  },
  {
   "fieldname": "provision_section",
   "fieldtype": "Section Break",
   "label": "Provision",
   "depends_on": "eval:doc.operation==\"Provision\""
  },
  {
   "fieldname": "provision_frappe_version",
   "fieldtype": "Select",
   "label": "Frappe Version",
   "options": "16\n15",
   "default": "16",
   "depends_on": "eval:doc.operation==\"Provision\"",
   "read_only": 1,
   "description": "Decides the branch cloned and the Python needed. v16 pins 3.14; v15 runs on 3.12."
  },
  {
   "fieldname": "provision_bench_name",
   "fieldtype": "Data",
   "label": "Bench Name",
   "depends_on": "eval:doc.operation==\"Provision\"",
   "read_only": 1,
   "description": "Becomes a directory under the bench root."
  },
  {
   "fieldname": "provision_site_name",
   "fieldtype": "Data",
   "label": "Site Name",
   "depends_on": "eval:doc.operation==\"Provision\"",
   "read_only": 1,
   "description": "Optional. Left empty the bench is built with no site on it."
  },
  {
   "fieldname": "provision_apps",
   "fieldtype": "Small Text",
   "label": "Apps",
   "depends_on": "eval:doc.operation==\"Provision\"",
   "read_only": 1,
   "description": "One per line, as profile|repo|branch. Fetched in order, then installed on the site in the same order \u2014 an app that depends on another has to come after it."
  },
  {
   "fieldname": "provision_port_index",
   "fieldtype": "Int",
   "label": "Port Block",
   "depends_on": "eval:doc.operation==\"Provision\"",
   "read_only": 1,
   "description": "The index this bench occupies: 5 means 8005, 9005, 11005 and 13005. Allocated at run time from what is already in use."
  },
  {
   "fieldname": "provision_skip_assets",
   "fieldtype": "Check",
   "label": "Skip Assets",
   "default": "1",
   "depends_on": "eval:doc.operation==\"Provision\"",
   "read_only": 1,
   "description": "On by default. An asset build is the largest memory consumer in the whole process, and a bench that dies at the last step has still spent four gigabytes."
  },
  {
   "fieldname": "provision_domain",
   "fieldtype": "Data",
   "label": "Domain",
   "depends_on": "eval:doc.operation==\"Provision\"",
   "read_only": 1
  },
  {
   "fieldname": "provision_domain_provider",
   "fieldtype": "Link",
   "options": "Domain Provider",
   "label": "Domain Provider",
   "depends_on": "eval:doc.operation==\"Provision\"",
   "read_only": 1,
   "description": "Left empty the DNS record is not written and the wizard tells you what to create by hand."
  },
  {
   "fieldname": "provision_db_password",
   "fieldtype": "Password",
   "label": "Database Root Password",
   "depends_on": "eval:doc.operation==\"Provision\"",
   "description": "bench new-site has no way to take this other than on the command line. Stored encrypted, redacted out of the logged command, and cleared once the job finishes."
  },
  {
   "fieldname": "provision_admin_password",
   "fieldtype": "Password",
   "label": "Administrator Password",
   "depends_on": "eval:doc.operation==\"Provision\"",
   "description": "For the new site's Administrator. Left empty frappe generates one and prints it in the log."
  }
 ],
 "index_web_pages_for_search": 0,
 "links": [],
 "modified": "2026-08-24 00:00:00",
 "modified_by": "Administrator",
 "module": "Server",
 "name": "App Install Request",
 "naming_rule": "Expression",
 "owner": "Administrator",
 "permissions": [
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "export": 1,
   "print": 1,
   "read": 1,
   "report": 1,
   "role": "System Manager",
   "share": 1,
   "write": 1
  }
 ],
 "sort_field": "creation",
 "sort_order": "DESC",
 "track_changes": 0
}