[project]
name = "server"
authors = [
    { name = "Carbonite Solutions Ltd", email = "admin@carbonitesolutions.com"}
]
description = "Managing the server and frappe benches "
requires-python = ">=3.14"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # "frappe~=16.0.0" # Installed and managed by bench.
    #
    # Deliberately empty, and worth keeping that way. Everything this app does
    # in Python uses the standard library: journalctl and git are driven through
    # subprocess, GitHub through urllib, and log parsing through re. That means
    # `bench get-app` can never fail here for a package that will not build, and
    # the app runs on the bench's own interpreter with nothing added.
    #
    # What it DOES need is operating-system level and cannot be expressed here:
    # git, ssh and (for the preferred log source) systemd's journalctl, plus a
    # bench user that can read the authentication log — on Debian and Ubuntu,
    # membership of the 'adm' group. `server/install.py` checks all of that at
    # install time and prints what is missing.
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"

# These apt dependencies will be installed from Ubuntu repositories when you host your app on Frappe Cloud
[deploy.dependencies.apt]
packages = []

[tool.ruff]
line-length = 110
target-version = "py314"

[tool.ruff.lint]
select = [
    "F",
    "E",
    "W",
    "I",
    "UP",
    "B",
    "RUF",
]
ignore = [
    "B017", # assertRaises(Exception) - should be more specific
    "B018", # useless expression, not assigned to anything
    "B023", # function doesn't bind loop variable - will have last iteration's value
    "B904", # raise inside except without from
    "E101", # indentation contains mixed spaces and tabs
    "E402", # module level import not at top of file
    "E501", # line too long
    "E741", # ambiguous variable name
    "F401", # "unused" imports
    "F403", # can't detect undefined names from * import
    "F405", # can't detect undefined names from * import
    "F722", # syntax error in forward type annotation
    "W191", # indentation contains tabs
    "UP030", # Use implicit references for positional format fields (translations)
    "UP031", # Use format specifiers instead of percent format
    "UP032", # Use f-string instead of `format` call (translations)
    "UP037", # quoted annotations
    "UP040", # Use type aliases instead of type annotations
]
typing-modules = ["frappe.types.DF"]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
docstring-code-format = true
